// 2.4 Passing arguments across the boundary

A syscall like read(fd , buff ,n )
needs to hand the kernel sveral values , there are three ways to pass paramteres , and the lecture care about why the simple one is NOT enought :

---

==============================

1. registers only

this is the fist and simple . it fails when therea re more arguments than registers . or when an argumenet is large , such as whole struct or a long string

register are too small -> not this way , cannot work

==============================

2. a block in memory , with its address in a register :

the kernel receives one register holding a pointer to the blick ,and reads the arguments from tehre . this is what linux does when therea re many parameteres . and it is why : read(df , buff , n)
can point at a alarge buffer .
it also rasies the safty quetsion .

the kernel is being handed a "\_user - supplied pointer"
so it must not blindly trust it

that is why XV6 uses : copyin / copyout and chekc address

==============================

3. the stack
   the suer pusheds the arguments and teh kernel pops them .
   it works but it needes the kernel to trues and read teh user's stack :

Call by number, not by name------

the user prorgam cannot say call function read() by name or address .
kernel addresses are hidden from user mode

instead , every syscall gest anumber -> in xv6 , a value in a header such as SYS_read
the user puts that number n a reguster -> use a7 in RISC-V before "ecall"

The kernel keeps a table indexed by number and looks up the handler .
the table is the door;s namnues , and the user can only pick the snumber form it

the benites is that the user never lears any kernel address , and an invalid number is easy to reject .

=================

my understanding :

Kernel and user have seprated virtual address spaces , so there is only a narrow way to pass arguments across.
passing everything in registers is not enought :

yjere are few registers and they are small , so they cannot hold big data .

so in practice we mix two ways :

- small data : like fd (file decipher) , and suscall nuber
  -> go directly into regsiter

- big data :
  stay in user memory , and we pass its address (pointer ) in register

The kernel cannot trues that pointer . It doesn;t let the user check it . it translat the address itself thorught users; page table

-> teh copyin / copyout

and check is valid and mapped if not , it returns an error

syscall are called by number , not by name:

the number goes into a regsiter (a7 in our xv6) and the kernel has a table indexed by number ot find the handler quickly .
this table chekc is only for : is this valid syscall ?
not for is this pointer valid ..
