//2.7 (GINI-specific) scause / sepc / SPP in practice

The three registers as clues to read after a trap:

- scause:
  why , the top bit syas interrupts vs exection and the lower bits give the specific casue
  tylical value are an ecall from user mode (code 8) and page faults
  code 12 , 13 , 15

-sepc :
the prorgam counter at the moment of the trap .
this is where excution will resume

SPP : which mode we came from
0 Means user mode
1 means supervisor (kernel) mode

=====================================

The exeriment: same timer interrupt , caught in two cases :

mode when it fire:

1. cpu busy :
   SPP 0 (from uer mode)  
   sepc points to : a user address inside prorgame
   entry code : uservec (via the trampoline)
   trapFrame written??
   -> yes

2. CPU idle (nothing runnable, scheduler looping in the kernel):
   SPP 1(from kernel mode)
   sepc points to a kernel address
   entry deo kernelvec
   Trapfram written?
   -> No (kernelvec saves registers on the kernel stack instead)

-The cause (scause) is the same timer interrupt in both.
What changes is where it landed, and therefore which path handles it.

---

The key lesson: "trapframe written" and "process switch happened" are separate events.

the traoFrame is saved on every trap from user mode .
that is unconditional , hardware and uservec do it before anything else is decided

whethere kernel then switches to a diff process is a seperated conditinal decision .
on a timer interrupt , the kernel may call yield() so the scheduler can pick someone else . but it isn;t fored to

so seeing a trapframe saved -> does not mean a context switch happened .
