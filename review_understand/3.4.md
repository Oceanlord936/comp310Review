//3.4 The Process Control Block (PCB)

PCB , the process control block is the kernel's data structire that holds everything it needs to knw about one process .
-> There is one pcb per process

and the kernel keeps tham ALL

====================================

what is inside:

1. ProcessID , teh PID
   and parent id

2. state -> ready , running , waiting , zombie .....

3. program counter and cpu regsiters , saved when the process is not running

4. memory information :
   page table , segment info

5. open files (file descriptors )

6. scheduling info :
   priority , time used

7. I/0 status and counting info

===========================
why nothing survives a countext switch unless it is in the PCB ???

When teh kernel switch away froma process , cpu's register and program counte r
get overwriten by the next process

the only way to resume th eold process later , exactly where it left off ,
if if its state was saved into its pcb first

so the PCB is what makes a process resumable
anythig not stored there is lost the moment the cpu runes something else .
