// 3.1 Program vs. process, and memory segments

program vs process:

a program is passive :
-> a file on disk , the executable just bytes

a process is that program in execution :
the code loaded in memort , plus its current state (program counter , register s ,s tack , open files)

one program can run as many process at once
-> two peopel running same editor in 2 process from on program

==================================
memory segment of a process:

1. text :
   the machine instructiosn -> fix size , read only

2. initallized data :
   global and static variables , that have a starting value
   -> size fixed in comple time

3. BSS
   global and static varible that has no initalized value
   -> no staored in file just size

4. HEAP :
   Hynamically allocated memory -> malloc()
   grow as the prgram ask for more

5. stack :
   function call frames : local variables , arguments return address
   grows and shrinks with call
