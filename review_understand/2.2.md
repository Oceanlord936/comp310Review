//2.2 What the trap does, every time

whatever the cause -> syscall (deliberated) , interupt (external) , fault ..

the hardware does teh same for things :

==================

1. save state : it records where teh program was , so it can resume later
   -> on RISC -V this is the program counter going into "sepc"

-what is "sepc" ???

sepc is a hardware register, not an assembly instruction.
-> it stands for : "supervisor exception program counter"
-> it is one of cpu's control register
on a trap , the hardware copies the current program counter into "sepc"
This is the "saved" address of the interrupted instruction

when kernel finsihed , "sret" copies sepc" back into the program couunter
-> the program resumes where it left off

--Registers like sepc, scause, stval and stvec all start with s because they belong to supervisor mode, the mode kernel runs in .

=======================

2.  record why :
    the cause goe sinto "scause" , which tells the kernel where this was a syscall , atimer interrupt , a page falt and so on .
    extra detail , such ast the faulting address -> hoes into "stval"

===========================

3. switch mode :
   the cpu goes from user to kernel (supervisor ) mode
   it remember the previou smode in "SPP" , so it knows where to return to

==========================

4. jump to a fixed entry point L
   the program counter is set to the address held in "stvec" ,
   which only the kenrel can write
